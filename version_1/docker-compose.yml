version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: kpi-chatbot-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: kpi_chatbot
    ports:
      - "3306:3306"
    volumes:
      - ./mysql-data:/var/lib/mysql

